# AMD MI300X (ROCm) deployment requirements
# Usage: pip install -r requirements-rocm.txt
#
# Key differences from requirements.txt:
#   - ROCm build of PyTorch (replaces CUDA build)
#   - NO bitsandbytes (not needed — MI300X has 192 GB HBM3 VRAM)
#   - accelerate stays (needed for device_map="auto")

--index-url https://download.pytorch.org/whl/rocm6.2
torch
torchvision
torchaudio

--index-url https://pypi.org/simple
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
aiosqlite>=0.20.0
alembic>=1.13.0
python-multipart>=0.0.9
slowapi>=0.1.9

# ML / AI
transformers>=4.41.0
accelerate>=0.30.0
pillow>=10.3.0

# Agents — CrewAI + LangChain backbone
crewai>=1.0.0
langchain>=0.2.0
langchain-community>=0.2.0

# SMS
africastalking>=1.0.0

# HTTP client
httpx>=0.27.0
